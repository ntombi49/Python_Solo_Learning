# them), we can cover these next:

# Default parameters → def greet(name="Friend"):

# Keyword arguments → greet(name="Ntombi", age=19)

# Returning multiple values → return x, y, z

# Functions calling other functions

# Scope (local vs global variables)

# Lambda (anonymous) functions

# Docstrings — writing descriptions for functions